name: local
gpus: 1
data_dir:
  cld: /mnt/work/mlpf/tensorflow_datasets/cld
  clic: /mnt/work/mlpf/tensorflow_datasets/clic
experiments_dir: experiments
environment:
  PF_SITE: local
runtime_overrides:
  dtype: bfloat16
  num_workers: 8
  prefetch_factor: 4
  model.attention.use_flash_attn_varlen: false
