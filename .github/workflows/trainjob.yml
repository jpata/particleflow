name: Test ARC

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: jpata-particleflow-custom-gpu1
    steps:
      - run: hostname
      - run: pwd
      - run: sleep 120
      - run: nvidia-smi
